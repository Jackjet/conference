using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace Conference_Conversation.Common
{
    public  class OldSolution
    {
        #region old solution

        //foreach (var item in conversationWindow.Conversation.Participants)
        //{
        //    item.BeginUnLockVideo(null, null);
        //}

        ////DispatcherTimer timer = null;
        //TimerJob.StartRun(new Action(() =>
        //{
        //  VideoWindow videoWindow = videoChannel.CaptureVideoWindow                       
        //    //判断是否可以执行该操作
        //    if (videoChannel.CaptureVideoWindow != null)
        //    {

        //        videoChannel.CaptureVideoWindow.AutoShow = 0;
        //        videoChannel.CaptureVideoWindow.FullScreenMode = 1;
        //        //停止计时器
        //        //timer.Stop();
        //    }
        //}), 2500);
        //videoChannel.CaptureVideoWindow

        //if (videoChannel.CanInvoke(ChannelAction.Resume))
        //{
        //    videoChannel.BeginResume(null, null);
        //}
        //if (avModality.CanInvoke(ModalityAction.Accept))
        //{
        //    avModality.Accept();
        //}

        #endregion
    }
}
